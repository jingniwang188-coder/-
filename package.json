{
  "scripts": {
    "ios:assets": "node tools/generate-ios-assets.mjs",
    "ios:prepare": "node tools/prepare-ios-web.mjs",
    "ios:sync": "npm run ios:assets && npm run ios:prepare && npx cap sync ios",
    "ios:open": "npm run ios:sync && npx cap open ios"
  },
  "dependencies": {
    "@capacitor/cli": "^8.3.4",
    "@capacitor/core": "^8.3.4",
    "@capacitor/ios": "^8.3.4",
    "@capacitor/local-notifications": "^8.2.0",
    "pg": "^8.20.0"
  },
  "devDependencies": {
    "pngjs": "^7.0.0"
  }
}
