{
  "scripts": {
    "ios:prepare": "node tools/prepare-ios-web.mjs",
    "ios:sync": "npm run ios:prepare && npx cap sync ios",
    "ios:open": "npm run ios:sync && npx cap open ios"
  },
  "dependencies": {
    "@capacitor/cli": "^8.3.4",
    "@capacitor/core": "^8.3.4",
    "@capacitor/ios": "^8.3.4",
    "pg": "^8.20.0"
  }
}
